from alpha_data import fetch_alpha_vantage_data, read_stock_symbols

STOCK_SYMBOLS = ['AAPL']
ENDPOINTS = {
    "TIME_SERIES_DAILY": {"symbol": None, "outputsize": "full", "datatype": "csv"},
    "WTI": {"interval": "daily", "datatype": "csv"}
}

df = fetch_alpha_vantage_data(symbols=STOCK_SYMBOLS, endpoints=ENDPOINTS, force_refresh=True)
print(df.head())
